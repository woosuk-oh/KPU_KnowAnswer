diff a/algorithm-game-app/src/app/app.component.html b/algorithm-game-app/src/app/app.component.html	(rejected hunks)
@@ -3,13 +3,15 @@
         <img src="../assets/images/title_m2.png" />
     </div>
 </header>
-<div *ngIf="selectedGame == null" class="grid grid-pad">
-    <a *ngFor="let game of games" [routerLink]="['/game', game.id]" class="col-mid-12" (click)="onSelect(game)">
-        <div class="game detail">
-            <h4>{{ game.detail }}</h4>
-        </div>
-        <div class="game image">
-            <img src=" {{game.imageUrl}}" />
+<div *ngIf="!selectedGame" class="grid grid-pad">
+    <a *ngFor="let game of games" [routerLink]="['/game', game.id]" routerLinkActive="active" class="col-mid-12" (click)="onSelect(game)">
+        <div class="game contents">
+            <div class="game detail">
+                <h4>{{ game.detail }}</h4>
+            </div>
+            <div class="game image">
+                <img src=" {{game.imageUrl}}" />
+            </div>
         </div>
     </a>
 </div>
